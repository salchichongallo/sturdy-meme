{
  "private": true,
  "name": "gatsby-v3-ie",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "gatsby develop",
    "build": "gatsby build",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public"
  },
  "dependencies": {
    "gatsby": "^3",
    "react": "^16",
    "react-device-detect": "^1.17.0",
    "react-dom": "^16"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
